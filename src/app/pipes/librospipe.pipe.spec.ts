import { LibrospipePipe } from './librospipe.pipe';

describe('LibrospipePipe', () => {
  it('create an instance', () => {
    const pipe = new LibrospipePipe();
    expect(pipe).toBeTruthy();
  });
});
