import { EmpleadopipePipe } from './empleadopipe.pipe';

describe('EmpleadopipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmpleadopipePipe();
    expect(pipe).toBeTruthy();
  });
});
